#include <stdio.h>

/**
 * main - Print the name of the program
 * @argc: number of args
 * @argv: array of command arguments
 * Return: 0 in success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0 * argc]);
	return (0);
}
